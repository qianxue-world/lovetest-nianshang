apiVersion: v1
kind: Service
metadata:
  name: lovetest-nianshang-service
  namespace: lovetest-nianshang-ns
  labels:
    app: lovetest-nianshang
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: lovetest-nianshang
